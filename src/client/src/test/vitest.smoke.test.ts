import { describe, expect, it } from "vitest";

// Verifies Vite and Vitest infrastructure are working correctly.
describe("vitest smoke test", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
