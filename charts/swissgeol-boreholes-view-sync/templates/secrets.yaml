{{- $existingSecret := (lookup "v1" "Secret" .Release.Namespace (printf "%s-secrets" .Release.Name)) }}
{{- $existing := dict }}
{{- if $existingSecret }}
  {{- $existing = $existingSecret.data }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  # Source database
  sourceDatabaseHost: {{ include "resolve-secret" (dict "override" .Values.db.source.host "existing" $existing "key" "sourceDatabaseHost" "valuesPath" "db.source.host") | quote }}
  sourceDatabasePort: {{ include "resolve-secret" (dict "override" .Values.db.source.port "existing" $existing "key" "sourceDatabasePort" "valuesPath" "db.source.port") | quote }}
  sourceDatabaseName: {{ include "resolve-secret" (dict "override" .Values.db.source.name "existing" $existing "key" "sourceDatabaseName" "valuesPath" "db.source.name") | quote }}
  sourceDatabaseSchema: {{ include "resolve-secret" (dict "override" .Values.db.source.schema "existing" $existing "key" "sourceDatabaseSchema" "valuesPath" "db.source.schema") | quote }}
  sourceDatabaseUsername: {{ include "resolve-secret" (dict "override" .Values.db.source.username "existing" $existing "key" "sourceDatabaseUsername" "valuesPath" "db.source.username") | quote }}
  sourceDatabasePassword: {{ include "resolve-secret" (dict "override" .Values.db.source.password "existing" $existing "key" "sourceDatabasePassword" "valuesPath" "db.source.password") | quote }}
  # Target database
  targetDatabaseHost: {{ include "resolve-secret" (dict "override" .Values.db.target.host "existing" $existing "key" "targetDatabaseHost" "valuesPath" "db.target.host") | quote }}
  targetDatabasePort: {{ include "resolve-secret" (dict "override" .Values.db.target.port "existing" $existing "key" "targetDatabasePort" "valuesPath" "db.target.port") | quote }}
  targetDatabaseName: {{ include "resolve-secret" (dict "override" .Values.db.target.name "existing" $existing "key" "targetDatabaseName" "valuesPath" "db.target.name") | quote }}
  targetDatabaseUsername: {{ include "resolve-secret" (dict "override" .Values.db.target.username "existing" $existing "key" "targetDatabaseUsername" "valuesPath" "db.target.username") | quote }}
  targetDatabasePassword: {{ include "resolve-secret" (dict "override" .Values.db.target.password "existing" $existing "key" "targetDatabasePassword" "valuesPath" "db.target.password") | quote }}
